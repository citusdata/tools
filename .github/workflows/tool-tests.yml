name: Tool Tests

env:
  GH_TOKEN: ${{ secrets.GH_TOKEN }}

on:
  push:
    branches: "**"

  workflow_dispatch:

jobs:
  unit_test_execution:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: sudo apt install libcurl4-openssl-dev libssl-dev

      - name: Define git credentails
        run: git config --global user.email "gindibay@microsoft.com"&& git config --global user.name "Gurkan Indibay"

      - name: install requirements
        run: python -m pip install -r python/requirements.txt

      - name: Unit tests for "Update Package Properties"
        run:  python -m pytest -q python/tests/test_update_package_properties.py

      - name: Unit tests for "Prepare Release"
        run: python -m pytest -q python/tests/test_prepare_release.py

      - name: Unit tests for "Update Docker"
        run: python -m pytest -q python/tests/test_update_docker.py



