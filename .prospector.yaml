test-warnings: true
doc-warnings: false
member-warnings: false

ignore-paths:
  - docs

autodetect: true

pylint:
  disable:
    - django-not-available
  options:
    max-line-length: 160
    max-args: 6
